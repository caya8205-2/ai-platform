{
    "name": "ai-platform",
    "private": true,
    "version": "0.0.1",
    "scripts": {
        "build": "pnpm -r build",
        "dev": "pnpm -r --parallel dev",
        "lint": "pnpm -r lint",
        "typecheck": "pnpm -r typecheck",
        "clean": "pnpm -r clean"
    },
    "devDependencies": {
        "typescript": "^5.4.5",
        "@types/node": "^20.12.0",
        "tsx": "^4.7.0"
    }
}